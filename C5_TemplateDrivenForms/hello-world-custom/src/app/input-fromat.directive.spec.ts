import { InputFromatDirective } from './input-fromat.directive';

describe('InputFromatDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFromatDirective();
    expect(directive).toBeTruthy();
  });
});
